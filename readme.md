# Weather CLI Application

A command-line application that interacts with the OpenWeather API to:
- Search weather for a city
- Add/remove favourite cities (max 3)
- List favourite cities with live weather

## Requirements
- Python 3.8+
- OpenWeather API key

## Setup
pip install -r requirements.txt

## Run
python weather_app.py
